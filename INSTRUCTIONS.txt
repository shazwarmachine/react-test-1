Open command prompt or git Bash on the react-js-1 directory.

1.run npm install
 
2.start server with `node server.js`

3. start react app with `npm start`

You need to open localhost:4350(or whichever localhost npm start opens) in multiple tabs for multiple USERS.